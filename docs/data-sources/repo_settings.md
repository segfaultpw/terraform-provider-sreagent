---
page_title: "sreagent_repo_settings Data Source - sreagent"
subcategory: ""
description: |-
  Every repo_setting row, read-only. One repository branch mapping for fix requests. fix_runner is per repository: every mapping of one repository must name the same runner. Creating one needs the organization's GitHub App to reach the repository.
---

# sreagent_repo_settings (Data Source)

Every repo_setting row, read-only. One repository branch mapping for fix requests. fix_runner is per repository: every mapping of one repository must name the same runner. Creating one needs the organization's GitHub App to reach the repository.

## Example Usage

```terraform
data "sreagent_repo_settings" "all" {}

output "repo_settings_count" {
  value = length(data.sreagent_repo_settings.all.items)
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `id` (String) The resource key the list was read from.
- `items` (Attributes List) The rows. (see [below for nested schema](#nestedatt--items))
- `truncated` (Boolean) True when the API returned as many rows as its cap and may hold more.

<a id="nestedatt--items"></a>
### Nested Schema for `items`

Read-Only:

- `branches` (String) The repository's branch mappings.
- `fix_runner` (String) The repository's fix runner.
- `id` (String) The row's identifier on the platform.
- `repo` (String) owner/name.
