---
page_title: "sreagent_service_bindings Data Source - sreagent"
subcategory: ""
description: |-
  Every service_binding row, read-only. Which log groups, traces and metrics belong to a service in the Observability Explorer.
---

# sreagent_service_bindings (Data Source)

Every service_binding row, read-only. Which log groups, traces and metrics belong to a service in the Observability Explorer.

## Example Usage

```terraform
data "sreagent_service_bindings" "all" {}

output "service_bindings_count" {
  value = length(data.sreagent_service_bindings.all.items)
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `id` (String) The resource key the list was read from.
- `items` (Attributes List) The rows. (see [below for nested schema](#nestedatt--items))
- `truncated` (Boolean) True when the API returned as many rows as its cap and may hold more.

<a id="nestedatt--items"></a>
### Nested Schema for `items`

Read-Only:

- `account_id` (String) The AWS account the service lives in; when set, logs and metrics are searched there only. Traces always search every account.
- `environment` (String) The environment; empty for the default.
- `id` (String) The row's identifier on the platform.
- `log_filter` (String) A filter pattern applied to them.
- `log_groups` (List of String) CloudWatch log groups.
- `metric_selectors` (String) Metric selectors, as jsonencode([...]).
- `region` (String) The AWS region the service lives in; pairs with account_id.
- `service` (String) The service. Stored trimmed and lowercased.
- `trace_service_names` (List of String) Service names in traces.
